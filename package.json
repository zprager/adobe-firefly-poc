{
  "name": "campaign-gen",
  "version": "0.1.0",
  "description": "CLI that generates social campaign assets: AI hero image, post + overlay copy, HTML composition, and rendered PNGs.",
  "type": "module",
  "bin": {
    "campaign-gen": "src/cli.js"
  },
  "scripts": {
    "start": "node src/cli.js",
    "generate": "node src/cli.js generate"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "dotenv": "^16.4.5",
    "openai": "^4.67.0",
    "puppeteer": "^23.5.0",
    "yaml": "^2.5.1",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=20"
  }
}
